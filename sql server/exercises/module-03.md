# Module 03 Exercises — Design

1. Draw ERD: Student, Course, Enrollment (M:N).
2. Normalize: `Order(OrderId, CustomerName, CustomerCity, ProductName, Qty)` — list resulting tables.
3. Draw context DFD for Library system (3 external entities max).
4. Run `sql/11` and list all foreign keys on `library.Loan`.
