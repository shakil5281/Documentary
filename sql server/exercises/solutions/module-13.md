# Module 13 Solutions

1. Azure SQL Database (PaaS single DB), SQL VM (IaaS full control), Managed Instance (near-full instance in VNet).
2. Lift-and-shift needing full SQL surface (agents, certain features).
3. Data Migration Assistant (DMA).
4. Shared compute pool across multiple databases for cost scaling small DBs.
