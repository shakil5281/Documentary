# Module 03 Solutions

1. Student —< Enrollment >— Course (junction Enrollment).
2. Customer(CustomerId, Name, City); Product(ProductId, Name); Order(OrderId, CustomerId, ...); OrderLine(OrderId, ProductId, Qty).
3. Member, Librarian → Library System → flows for loan/return.
4. `FK_Loan_Member`, `FK_Loan_Book` (see script 11).
