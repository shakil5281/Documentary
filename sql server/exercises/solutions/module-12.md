# Module 12 Solutions

1. SQL Server Agent service.
2. `run_status = 1` (succeeded).
3. `IF COL_LENGTH('dbo.T','Col') IS NULL ALTER TABLE dbo.T ADD Col ...`
4. Silent backup/maintenance failures cause data loss until discovered.
