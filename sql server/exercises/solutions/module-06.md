# Module 06 Solutions

1. `Server=localhost;Database=LearnSQL;Trusted_Connection=True;TrustServerCertificate=True;`
2. Reuses TCP/logins; lowers overhead vs new connection per request.
3. READ COMMITTED (and SNAPSHOT/REPEATABLE READ/SERIALIZABLE); not READ UNCOMMITTED.
4. SQL injection; data breach; unauthorized DML.
