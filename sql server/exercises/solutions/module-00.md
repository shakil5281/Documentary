# Module 00 Solutions

1. Developer (full dev), Express (limited free), Enterprise (production). You likely installed Developer or Express.
2. Instance = one engine install; database = container for tables inside that instance.
3. Answers from your `SELECT` output.
4. Windows Authentication (domain/PC identity); SQL Authentication (username/password for apps/cross-domain).
