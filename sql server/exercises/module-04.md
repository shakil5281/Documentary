# Module 04 Exercises

1. Create view `dbo.vw_OpenOrders` (StatusCode = 'OP').
2. Procedure `usp_CountOrdersByCustomer @CustomerId` returning count.
3. Transaction: insert order + line; rollback on invalid ProductId.
4. Why avoid business logic only in triggers?
