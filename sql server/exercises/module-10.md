# Module 10 Exercises — Monitoring

1. Query blocking sessions from `sys.dm_exec_requests`.
2. Top 5 wait types from `sys.dm_os_wait_stats` (filter noise).
3. Run Extended Events lab script 19 — what event did you capture?
4. When would you use Extended Events vs only Activity Monitor?
