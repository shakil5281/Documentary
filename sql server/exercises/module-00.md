# Module 00 Exercises — Install

1. List three SQL Server editions and which you installed.
2. What is the difference between an **instance** and a **database**?
3. Run `00-verify-instance.sql` and record `Edition` and `ServerName`.
4. Name two authentication modes and when to use each.
