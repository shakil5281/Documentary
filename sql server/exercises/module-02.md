# Module 02 Exercises — T-SQL

1. Add column `City NVARCHAR(100)` to `dbo.Customer`.
2. Insert one customer; update their city; delete only if `CustomerId` matches test row.
3. Customers with **no** orders (`LEFT JOIN`).
4. Revenue per customer (`GROUP BY`).
5. Write a CTE that lists products never ordered.
