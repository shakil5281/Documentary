# Module 12 Exercises — Automation

1. What SQL Server service must run for Agent jobs?
2. Run `20-agent-job-lab.sql` — what is `run_status` when job succeeds?
3. Write one idempotent `ALTER TABLE` pattern for adding a column.
4. Why notify on job failure?
