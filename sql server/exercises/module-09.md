# Module 09 Exercises — Security

1. Create login `ReportUser` and grant SELECT on `dbo.Customer` only.
2. Difference between `GRANT` and `DENY`?
3. Execute as `Learn_AppReader` — confirm DELETE fails on Order.
4. List three production security practices.
