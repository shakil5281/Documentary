# Module 11 Exercises — High Availability

1. Compare Always On AG vs log shipping in a table (RPO, failover, read scale).
2. Run `23-ha-concepts-readonly.sql` — what does `IsHadrEnabled` show on your instance?
3. When would you use table partitioning vs read replica?
4. Define RPO/RTO for a payment database (example values).
